extends layout

block content
  a(href=item.category.url)
    button Back to #{item.category.name}
  h1= item.name
  p= item.description
  p Quantity: #{item.stock}

  p
    a(href=item.url + '/update') Edit item
  p
    a(href=item.url + '/delete') Delete item